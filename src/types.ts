export type User = {
    id: string,
    name: string,
    email: string,
    password: string,
    createdAt: string
}

export type UserDB = {
    id: string,
    name:string,
    email: string,
    password:string,
    created_at: string
}